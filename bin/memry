#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/cli'

require 'thor'

%w[SIGINT INT].each do |signal|
  trap signal do
    puts
    exit
  end
end

loop do
  input = Thor::Shell::Basic.new.ask 'Input:', :yellow

  CLI.new.quit if input.nil?

  CLI.start(input.split)
end
